[uwsgi]
protocol = uwsgi
;module = server
;callable = server
wsgi-file= app/src/server.py
master = true
enable-threads = true
threads = 2
thunder-lock = true
; Set uWSGI to start up 5 workers
processes = 5
; We use the port 8000 which we will

socket = 0.0.0.0:8000
vacuum = true
